class Account {
    constructor(name, pin, icon) {
        this.name = name; // Name of the account
        this.pin = pin;   // PIN for this account, type is String
        this.icon = icon; // Instance of Icon, profile image
    }
}
