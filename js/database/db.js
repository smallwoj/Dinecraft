window.DB = {
    icons: [],
    accounts: [],
    menuItems: [],
    tables: [],
};

// In general each part of the DB should have some unique gets like: getByName(name) and others when required

// This will fill window.DB with all the fake data we need
window.DB.createFakeData = function() {
    // This is kinda what we need:
    // icons.push(new Icon(name string, url string));
    // accounts.push(new Account(name string, PIN numbr, icon name string));
};


